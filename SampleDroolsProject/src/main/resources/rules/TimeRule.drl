//created on: May 27, 2014
package sample

import com.sample.TimeObj;

rule "Your First Rule"
    when
      $time : TimeObj(value != currentTime)
    then
      modify($time){
            setValue($time.getCurrentTime());
        };

end

